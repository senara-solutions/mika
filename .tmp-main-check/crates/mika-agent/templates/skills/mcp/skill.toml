[skill]
name = "mcp"
description = "Explain MCP (Model Context Protocol) server configuration"
version = "0.1.0"
always_on = false
timeout_secs = 10

[triggers]
keywords = ["model context protocol", "mcp server", "mcp tool"]
