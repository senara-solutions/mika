# Learnings review — mika#2107 (guard-shared-checkout)

## 1. Patterns HONOURED

- `docs/solutions/best-practices/fire-disposition-doctrine.md` (mika#1574) — the plan's Fire-Disposition table (`GUARD_EXCEPTION_ALLOWLIST=()`) is scoped inside `scripts/test-guard-shared-checkout.sh` only, with a companion assertion that the production script consults no such table — this is the bash-shell equivalent of the doctrine's `#[cfg(test)] mod tests` requirement, faithfully applied (zero-entry case is a legitimate instance of Option (a), not a misuse).
- `docs/solutions/architecture-patterns/guard-parser-must-be-as-permissive-as-downstream-consumer-2026-08-29.md` (mika#2046) — the guard's test suite deliberately targets the exact bypass class this doc warns about (parser stricter than the real consumer): `-C`, `--git-dir`, `GIT_DIR=`, `GIT_WORK_TREE=`, cd-tracking, command chaining via `&&`/`;`, and multi-line commands are all covered as deny fixtures, not just the nominal form.
- `docs/solutions/best-practices/un-label-denforcement-non-declare-echoue-en-silence-2026-09-01.md` (mika#2123) — `scripts/check-shared-checkout-guard-wiring.sh` reads the actual registry (`.claude/settings.json` is tracked, matcher covers Bash, SessionStart present) rather than trusting the calling code's intent, matching this doc's "read the registry, not the code" remedy.
- Same doc + `docs/solutions/architecture-patterns/2026-09-06-accesseur-etroit-a-cote-du-resolveur-canonique.md` (mika#2205) — the SessionStart "armed" log line is exactly the discriminator mika#2205 says was missing for `auto_pull`/`wip_rescue`: without it, "nothing to refuse" and "nothing armed" are indistinguishable. This guard ships that discriminator from day one.
- `docs/solutions/best-practices/a-guard-must-observe-not-assert-2026-08-29.md` (mika#2041) and `docs/solutions/best-practices/structural-guard-fails-open-parser-fixture-harness.md` (mika#2039) — the test harness builds real git repos per-fixture (`build_repo_pair`) rather than fetching fixed state from git history (the exact anti-pattern mika#2039's doc warns against), and explicitly tests fail-open paths (unwritable log, missing guard script) as their own named scenarios rather than assuming green means safe.
- CONTRIBUTING.md's framing of this guard ("le garde-fou") as deliberately distinct from mika#2141's sandbox containment ("le mur") keeps scope narrow rather than duplicating dispatch-containment's job — consistent with this repo's general pattern of naming what a mechanism does NOT cover (see mika#2189/#2342 "Out of scope, deliberately" sections).

## 2. Recorded rules CONTRADICTED

None found. Across all six threads searched (permissive-parser doctrine, silent-enforcement-registry doctrine, the mika#2205/2327-class "silently-inactive mechanism," Fire-Disposition, mika#2141 containment scoping, and a search for an "expected regime / halt condition" doctrine specifically — no single dedicated doc exists for that last one; it appears only as a pervasive convention across many `CLAUDE.md` env-var sections, not as a citable `docs/solutions/` rule) turned up a doctrine this change violates. The guard's own design choices (armed-line discriminator, permissive detection/strict decision split, test-scoped allowlist, synthesized fixtures) each land on the side the repo's prior incidents say to land on.

## 3. Tried before / built differently

No `docs/solutions/` entry discusses a Claude Code `PreToolUse`/`SessionStart` hook as a mechanism — this is a first for that specific enforcement surface in this repo's institutional memory, so there is no prior failure to check it against. The two closest-precedent classes are the ones cited above (mika#2039's three-layer fail-open audit, mika#2041's observe-vs-assert distinction); nothing in either suggests this guard class (a hook fronting `git`) should be built structurally differently than it is — both docs' remedies are the ones this guard already applies.
