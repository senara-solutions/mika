# Allowlist for scripts/check-a2a-timeout-literals.sh rule A2 (mika#2309).
#
# Format, one per line:
#
#     <path-relative-to-repo-root>:<CONST_NAME>  # why this const is not an env default
#
# The reason is not decoration. It is what a future reader needs in order to
# re-audit the site, and an entry without one is indistinguishable from a
# forgotten exemption.
#
# COMPARED IN BOTH DIRECTIONS. An entry that no longer matches a real A2
# violation fails the build exactly as an unlisted site does. An allowlist
# nobody prunes becomes the junk drawer in which the regression this guard
# exists to catch passes unnoticed — see the § M1 table in
# docs/plans/2026-09-18-004-feat-2309-prevol-d-en-garde-ci-plan.md.
#
# SHIPPED EMPTY, and measured empty on 2026-09-18 (e1342dfa): the declared
# perimeter holds exactly two `const … : Duration` — `DEFAULT_TIMEOUT` (600 s,
# compliant: DEFAULT_* + TIMEOUT_ENV) and `RECOVERY_TIMEOUT` (30 s, below the
# threshold). Zero exemptions is the intended steady state, not an accident of
# timing.
