[skill]
name = "skill-review"
description = "Generate model-tuned system_prompt.md variants for skills"
version = "0.1.0"
always_on = false
timeout_secs = 60

[triggers]
keywords = []

[constraints]
required_tools = ["review_skill"]
