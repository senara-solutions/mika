[skill]
name = "github"
description = "Interact with GitHub using the gh CLI"
version = "0.1.0"
always_on = false
timeout_secs = 30

[triggers]
keywords = ["github", "pull request", "pull requests", "open pr", "my prs", "pr review", "pr checks", "github issue", "file an issue", "repo status", "github release", "github actions", "ci checks", "ci pipeline", "merge pr", "git branch"]
